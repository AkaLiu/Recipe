---
interface Props {
  prepTime?: string;
  cookTime?: string;
  bakeTime?: string;
  coolTime?: string;
  servings?: number;
  robot?: string;
}

const { prepTime, cookTime, bakeTime, coolTime, servings, robot } = Astro.props;
---

<div class="flex flex-wrap gap-3 text-sm">
  {prepTime && (
    <div class="flex items-center gap-2 bg-terracotta-50 px-3 py-2 rounded-lg border border-terracotta-100">
      <svg class="w-5 h-5 text-terracotta-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" />
      </svg>
      <span class="text-gray-700">准备: <strong>{prepTime}</strong></span>
    </div>
  )}

  {cookTime && (
    <div class="flex items-center gap-2 bg-orange-50 px-3 py-2 rounded-lg border border-orange-100">
      <svg class="w-5 h-5 text-orange-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17.657 18.657A8 8 0 016.343 7.343S7 9 9 10c0-2 .5-5 2.986-7C14 5 16.09 5.777 17.656 7.343A7.975 7.975 0 0120 13a7.975 7.975 0 01-2.343 5.657z" />
      </svg>
      <span class="text-gray-700">烹饪: <strong>{cookTime}</strong></span>
    </div>
  )}

  {bakeTime && (
    <div class="flex items-center gap-2 bg-sage-50 px-3 py-2 rounded-lg border border-sage-100">
      <svg class="w-5 h-5 text-sage-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z" />
      </svg>
      <span class="text-gray-700">烘烤: <strong>{bakeTime}</strong></span>
    </div>
  )}

  {coolTime && (
    <div class="flex items-center gap-2 bg-blue-50 px-3 py-2 rounded-lg border border-blue-100">
      <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M13 10V3L4 14h7v7l9-11h-7z" />
      </svg>
      <span class="text-gray-700">冷却: <strong>{coolTime}</strong></span>
    </div>
  )}

  {servings && (
    <div class="flex items-center gap-2 bg-cream-100 px-3 py-2 rounded-lg border border-cream-200">
      <svg class="w-5 h-5 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
      </svg>
      <span class="text-gray-700">份量: <strong>{servings}人份</strong></span>
    </div>
  )}

  {robot && (
    <div class="flex items-center gap-2 bg-purple-50 px-3 py-2 rounded-lg border border-purple-100">
      <span class="text-lg">🤖</span>
      <span class="text-gray-700">{robot}</span>
    </div>
  )}
</div>
